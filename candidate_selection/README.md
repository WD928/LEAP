# Candidate Selection Tables

`top50_validation_shortlists_mechanism_scores.csv` contains round-specific top-50 validation shortlists. The table includes round number, top-50 rank, molecule name, CAS number, SMILES string, and mechanism-score summaries.
